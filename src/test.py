import numpy as np

if __name__ == '__main__':
    # Create an error: due to the length of LOC excess 80 characters
    # print('Check length of a single line of code (LOC),
    # and we expect this LOC is too long because
    # it contains more than 80 characters:', np.array([1, 2, 3, 4, 5, 6, 7, 8]))
    # Fix the error by breaking the LOC down
    print('Check length of a single line of code (LOC), '
          'and we expect this LOC is too long '
          'because it contains more than 80 characters:',
          np.array([1, 2, 3, 4, 5, 6, 7, 8]))
